language: C
group:    stable
dist:     precise
os:       linux

compiler:
  - clang
  - gcc

before_script:
  - sudo apt-get -qq update
  - sudo apt-get install -y automake autoconf
  - bash autogen.sh
